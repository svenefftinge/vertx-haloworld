package com.shinetech.haloworld.hal;

/**
 * List of possible message types (used for switching on the client side).
 */
public enum MessageType {

    LOG,
    FEED_RESULT,
    FEED_RESULT_UPDATE,
    CHAT,
    MEMBER_JOINED

}
